import Counters from './counters';

const CountersContainer = ({ collectData, formData }: any) => {
  return <Counters collectData={collectData} formData={formData} />;
};

export default CountersContainer;
